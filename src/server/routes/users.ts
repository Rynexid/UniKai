import { Hono } from "hono";

/**
 * Placeholder route handler domain `users`.
 * Implementasi menyusul - mount di src/server/index.ts ketika sudah siap.
 */
const router = new Hono();

export default router;
