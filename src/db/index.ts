/*
 * Barrel skema DB.
 * Catatan: klien runtime (db) pindah ke infrastructure/database - lihat
 * infrastructure/database/client.ts. Di sini hanya skema + relasi + tipe.
 */
export * from "./schema";
